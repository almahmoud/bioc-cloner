nameOverride: ""
fullnameOverride: ""

extraFileMappings: {}

configs:
  hash_and_push.sh: |
    #!/bin/sh
    mkdir -p alllogs
    ls -lh $1 >> alllogs/ls
    md5sum $1 >> alllogs/md5
    sha256sum $1 >> alllogs/sha256
    rclone copyto $1 iujs:biorefdata/bioconductor/v1/packages/$2 -v 2>&1 >> alllogs/iujs.log
    rclone copyto $1 taccjs:biorefdata/bioconductor/v1/packages/$2 -v 2>&1 >> alllogs/taccjs.log
    rclone copyto $1 osn:bir190004-bucket01/biorefdata/bioconductor/v1/packages/$2 -v 2>&1 >> alllogs/osn.log
    rclone copyto $1 awsopendata:biorefdata-dev/bioconductor/v1/packages/$2 -v 2>&1 >> alllogs/awsopendata.log
    rclone copyto alllogs awsopendata:biorefdata-dev/bioconductor-meta/v1/packages/$2/logs -v
    rclone copyto alllogs osn:bir190004-bucket01/biorefdata/bioconductor-meta/v1/packages/$2/logs -v
    rclone copyto alllogs taccjs:biorefdata/bioconductor-meta/v1/packages/$2/logs -v
    rclone copyto alllogs iujs:biorefdata/bioconductor-meta/v1/packages/$2/logs -v

  pull_and_upload.sh: |
    #!/bin/sh
    set -x
    mkdir -p "~/$(dirname $1)";
    BASE_URL="https://bioconductor.statistik.tu-dortmund.de/packages";
    cd "~/$(dirname $1)" && wget -O $(basename $1) "$BASE_URL/$1" && ls -lh . && echo $(pwd) && /bin/sh /mnt/configs/hash_and_push.sh $(basename $1) $1

cloneJob:
  image: rclone/rclone:latest
  command: cat /mnt/configs/list.txt | xargs -I@ /bin/sh /mnt/configs/pull_and_upload.sh @
  extraEnv: []
